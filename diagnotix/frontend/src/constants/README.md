# src/constants

Application-wide constants.

`nodeColors.ts` — maps each KG node type to a hex colour used in the sidebar legend and as the node fill colour in `GraphCanvas`. Also exports `DEFAULT_NODE_COLOR` for unrecognised types.
